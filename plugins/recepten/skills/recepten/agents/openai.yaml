interface:
  display_name: "Recepten"
  short_description: "Zoek, lees en beheer je receptenarchief"
  default_prompt: "Gebruik $recepten om een passend recept te vinden."

dependencies:
  tools:
    - type: "mcp"
      value: "recepten"
      description: "Beveiligde MCP-server voor het gedigitaliseerde receptenarchief"
      transport: "streamable_http"
      url: "https://recepten.roanensven.nl/mcp"

policy:
  allow_implicit_invocation: true
